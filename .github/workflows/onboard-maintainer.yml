name: Onboard Reviewer

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  issues: write

jobs:
  onboard:
    if: >-
      github.event.label.name == 'approved' &&
      contains(toJSON(github.event.issue.labels.*.name), 'maintainer-application')
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Parse issue body
        id: parse
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.issue.body;

            // GitHub issue forms use "### Label\n\nValue\n\n" format
            function extractField(label) {
              const regex = new RegExp(`### ${label}\\s*\\n\\n([^\\n]+)`, 'i');
              const match = body.match(regex);
              if (!match) {
                core.setFailed(`Could not find field: ${label}`);
                return '';
              }
              return match[1].trim();
            }

            const englishName = extractField('English Name');
            const chineseName = extractField('Chinese Name');
            const githubUsername = extractField('GitHub Username');
            const affiliation = extractField('Affiliation');
            const shortAffiliation = extractField('Short Affiliation');
            const domain = extractField('Domain');
            const subdomainRaw = extractField('Subdomain');

            // Validate github username: alphanumeric and hyphens only
            if (!/^[a-zA-Z0-9-]+$/.test(githubUsername)) {
              core.setFailed(`Invalid GitHub username: ${githubUsername}`);
              return;
            }

            // Subdomain dropdown value is "Domain / Display Name"
            const subdomainDisplay = subdomainRaw.includes(' / ')
              ? subdomainRaw.split(' / ').slice(1).join(' / ')
              : subdomainRaw;

            // Convert display name to folder name: lowercase, spaces to hyphens, remove special chars
            const subdomainFolder = subdomainDisplay
              .toLowerCase()
              .replace(/[^a-z0-9\s-]/g, '')
              .replace(/\s+/g, '-')
              .replace(/-+/g, '-')
              .replace(/^-|-$/g, '');

            // Map domain to emoji
            const emojiMap = {
              'Physics': '⚛️',
              'Mathematics': '➗',
              'Computer Science': '💻',
              'Quantitative Biology': '🧬',
              'Statistics': '📊',
              'EESS': '⚡',
              'Economics': '📈',
              'Quantitative Finance': '💹',
            };

            // Map domain to folder name
            const folderMap = {
              'Physics': 'physics',
              'Mathematics': 'mathematics',
              'Computer Science': 'computer-science',
              'Quantitative Biology': 'quantitative-biology',
              'Statistics': 'statistics',
              'EESS': 'eess',
              'Economics': 'economics',
              'Quantitative Finance': 'quantitative-finance',
            };

            // Validate domain
            if (!folderMap[domain]) {
              core.setFailed(`Invalid domain: ${domain}`);
              return;
            }

            const emoji = emojiMap[domain] || '';
            const domainFolder = folderMap[domain];

            core.setOutput('english_name', englishName);
            core.setOutput('chinese_name', chineseName);
            core.setOutput('github_username', githubUsername);
            core.setOutput('affiliation', affiliation);
            core.setOutput('short_affiliation', shortAffiliation);
            core.setOutput('domain', domain);
            core.setOutput('emoji', emoji);
            core.setOutput('domain_folder', domainFolder);
            core.setOutput('subdomain_display', subdomainDisplay);
            core.setOutput('subdomain_folder', subdomainFolder);

            console.log(`Parsed: ${englishName} (${chineseName}), @${githubUsername}`);
            console.log(`Domain: ${domain} (${domainFolder}), Subdomain: ${subdomainDisplay} (${subdomainFolder})`);

      - name: Update CODEOWNERS
        env:
          DOMAIN_FOLDER: ${{ steps.parse.outputs.domain_folder }}
          SUBDOMAIN_FOLDER: ${{ steps.parse.outputs.subdomain_folder }}
          USERNAME: ${{ steps.parse.outputs.github_username }}
        run: |
          CODEOWNERS_PATH=".github/CODEOWNERS"
          NEW_LINE="skills/${DOMAIN_FOLDER}/${SUBDOMAIN_FOLDER}/  @${USERNAME} @HHHHHejia"

          # Check if this subdomain line already exists
          if grep -q "^skills/${DOMAIN_FOLDER}/${SUBDOMAIN_FOLDER}/" "$CODEOWNERS_PATH"; then
            echo "CODEOWNERS entry for ${DOMAIN_FOLDER}/${SUBDOMAIN_FOLDER} already exists, updating..."
            sed -i "s|^skills/${DOMAIN_FOLDER}/${SUBDOMAIN_FOLDER}/.*|${NEW_LINE}|" "$CODEOWNERS_PATH"
          else
            # Insert after the domain-level line
            DOMAIN_LINE=$(grep -n "^skills/${DOMAIN_FOLDER}/ " "$CODEOWNERS_PATH" | head -1 | cut -d: -f1)
            if [ -n "$DOMAIN_LINE" ]; then
              sed -i "${DOMAIN_LINE}a\\${NEW_LINE}" "$CODEOWNERS_PATH"
            else
              # If no domain-level line found, append to end
              echo "$NEW_LINE" >> "$CODEOWNERS_PATH"
            fi
          fi

          echo "Updated CODEOWNERS:"
          cat "$CODEOWNERS_PATH"

      - name: Update readme.md English table
        env:
          EMOJI: ${{ steps.parse.outputs.emoji }}
          DOMAIN: ${{ steps.parse.outputs.domain }}
          SUBDOMAIN: ${{ steps.parse.outputs.subdomain_display }}
          ENGLISH_NAME: ${{ steps.parse.outputs.english_name }}
          USERNAME: ${{ steps.parse.outputs.github_username }}
          AFFILIATION: ${{ steps.parse.outputs.affiliation }}
        run: |
          NEW_ROW="| ${EMOJI} ${DOMAIN} | ${SUBDOMAIN} | ${ENGLISH_NAME} [@${USERNAME}](https:\/\/github.com\/${USERNAME}) | ${AFFILIATION} |"
          sed -i "/^> Want to maintain a subdomain/i\\${NEW_ROW}" readme.md
          echo "Updated English reviewer table"

      - name: Update readme.md Chinese table
        env:
          EMOJI: ${{ steps.parse.outputs.emoji }}
          DOMAIN: ${{ steps.parse.outputs.domain }}
          SUBDOMAIN: ${{ steps.parse.outputs.subdomain_display }}
          CHINESE_NAME: ${{ steps.parse.outputs.chinese_name }}
          USERNAME: ${{ steps.parse.outputs.github_username }}
          AFFILIATION: ${{ steps.parse.outputs.affiliation }}
        run: |
          NEW_ROW="| ${EMOJI} ${DOMAIN} | ${SUBDOMAIN} | ${CHINESE_NAME} [@${USERNAME}](https:\/\/github.com\/${USERNAME}) | ${AFFILIATION} |"
          sed -i "/^> 想认领一个子领域/i\\${NEW_ROW}" readme.md
          echo "Updated Chinese reviewer table"

      - name: Update docs/index.html
        uses: actions/github-script@v7
        env:
          SUBDOMAIN_DISPLAY: ${{ steps.parse.outputs.subdomain_display }}
          CHINESE_NAME: ${{ steps.parse.outputs.chinese_name }}
          USERNAME: ${{ steps.parse.outputs.github_username }}
          SHORT_AFFILIATION: ${{ steps.parse.outputs.short_affiliation }}
        with:
          script: |
            const fs = require('fs');
            const filePath = 'docs/index.html';
            let content = fs.readFileSync(filePath, 'utf8');

            const subdomainDisplay = process.env.SUBDOMAIN_DISPLAY;
            const chineseName = process.env.CHINESE_NAME;
            const username = process.env.USERNAME;
            const shortAffiliation = process.env.SHORT_AFFILIATION;

            const maintainerValue = `${chineseName} @${username} (${shortAffiliation})`;

            // Escape the subdomain name for use in regex
            const escapedName = subdomainDisplay.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

            // Check if this subdomain node already has a maintainer field
            const withMaintainerRegex = new RegExp(
              `(\\{ name: "${escapedName}",.*?)maintainer: "[^"]*"`,
              'g'
            );

            if (withMaintainerRegex.test(content)) {
              // Update existing maintainer
              content = content.replace(withMaintainerRegex, `$1maintainer: "${maintainerValue}"`);
              console.log(`Updated existing maintainer for "${subdomainDisplay}"`);
            } else {
              // No maintainer field yet — add it
              const lines = content.split('\n');
              let modified = false;

              for (let i = 0; i < lines.length; i++) {
                if (lines[i].includes(`name: "${subdomainDisplay}"`) && !modified) {
                  const line = lines[i];

                  if (line.includes('children:')) {
                    // Parent node: insert maintainer before ", children:"
                    lines[i] = line.replace(
                      /, children:/,
                      `, maintainer: "${maintainerValue}", children:`
                    );
                    modified = true;
                  } else if (line.match(/\}\s*,?\s*$/)) {
                    // Leaf node: insert maintainer before closing }
                    lines[i] = line.replace(
                      /(\s*)\}\s*(,?)\s*$/,
                      `, maintainer: "${maintainerValue}" $1}$2`
                    );
                    // Clean up extra spaces
                    lines[i] = lines[i].replace(/\s+\}/, ' }');
                    modified = true;
                  }
                }
              }

              if (modified) {
                content = lines.join('\n');
                console.log(`Added maintainer for "${subdomainDisplay}"`);
              } else {
                console.log(`WARNING: Could not find node for "${subdomainDisplay}" in index.html`);
              }
            }

            fs.writeFileSync(filePath, content);

      - name: Commit and push
        env:
          USERNAME: ${{ steps.parse.outputs.github_username }}
          DOMAIN_FOLDER: ${{ steps.parse.outputs.domain_folder }}
          SUBDOMAIN_FOLDER: ${{ steps.parse.outputs.subdomain_folder }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/CODEOWNERS readme.md docs/index.html
          git commit -m "chore: onboard @${USERNAME} as ${DOMAIN_FOLDER}/${SUBDOMAIN_FOLDER} reviewer"
          git push

      - name: Close issue with comment
        uses: actions/github-script@v7
        env:
          MAINTAINER_USERNAME: ${{ steps.parse.outputs.github_username }}
          MAINTAINER_DOMAIN: ${{ steps.parse.outputs.domain }}
          MAINTAINER_SUBDOMAIN: ${{ steps.parse.outputs.subdomain_display }}
        with:
          script: |
            const username = process.env.MAINTAINER_USERNAME;
            const domain = process.env.MAINTAINER_DOMAIN;
            const subdomain = process.env.MAINTAINER_SUBDOMAIN;

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: [
                `Welcome aboard, @${username}!`,
                ``,
                `You have been onboarded as the **${domain} / ${subdomain}** reviewer.`,
                ``,
                `**What was updated:**`,
                `- \`.github/CODEOWNERS\` — PRs touching your subdomain will now request your review`,
                `- \`readme.md\` — your name appears in both the English and Chinese reviewer tables`,
                `- \`docs/index.html\` — your name appears on the interactive knowledge tree`,
                ``,
                `**Next step:** Please check your GitHub notifications and **accept the collaborator invitation** for this repository so you can review and approve PRs.`,
                ``,
                `Thank you for contributing your expertise to OpenScientist!`,
              ].join('\n'),
            });

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              state: 'closed',
            });
